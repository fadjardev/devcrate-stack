# =============================================================
# supplier.qhomedata.id.test
# Project root : E:\dev\projects\supplier.qhomedata.id
# Web root     : project root  (CodeIgniter 2)
# PHP          : 7.4  â†’  127.0.0.1:9074
# Cert         : _wildcard.qhomedata.id.test  (covers *.qhomedata.id.test)
# =============================================================

server {
    listen       80;
    server_name  supplier.qhomedata.id.test;
    return 301   https://$host$request_uri;
}

server {
    listen       443 ssl;
    server_name  supplier.qhomedata.id.test;

    root   E:/dev/projects/supplier.qhomedata.id;
    index  index.php index.html;

    ssl_certificate      E:/dev/nginx-1.31.1/conf/certs/_wildcard.qhomedata.id.test.pem;
    ssl_certificate_key  E:/dev/nginx-1.31.1/conf/certs/_wildcard.qhomedata.id.test-key.pem;

    access_log  E:/dev/nginx-1.31.1/logs/supplier.qhomedata.id.test.access.log  main;
    error_log   E:/dev/nginx-1.31.1/logs/supplier.qhomedata.id.test.error.log   warn;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Block direct access to CI2 internals
    location ~ ^/(application|system)/ {
        deny all;
    }

    location ~ \.php$ {
        try_files       $uri =404;
        fastcgi_pass    127.0.0.1:9074;
        fastcgi_index   index.php;
        include         fastcgi_params;
        fastcgi_param   SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        fastcgi_param   HTTPS            on;
    }

    location ~ /\. {
        deny all;
    }
}
